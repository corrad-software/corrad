name: Deploy to EC2

on:
  push:
    branches:
      - lzs-corradai

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build application
        run: yarn build
        env:
          NUXT_ENV: production
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          NUXT_ACCESS_TOKEN_SECRET: ${{ secrets.NUXT_ACCESS_TOKEN_SECRET }}
          NUXT_REFRESH_TOKEN_SECRET: ${{ secrets.NUXT_REFRESH_TOKEN_SECRET }}
          PORT: 3000
          CHROMADB_URL: ${{ secrets.CHROMADB_URL }}
          APACHE_TIKA_URL: ${{ secrets.APACHE_TIKA_URL }}
          APP_URL: ${{ secrets.APP_URL }}
          SERVER: true

      - name: Initial EC2 Setup
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            # Install required software
            curl -sL https://rpm.nodesource.com/setup_18.x | sudo bash -
            sudo yum install -y nodejs
            sudo npm install -g yarn pm2
            
            # Create and setup project directory
            sudo mkdir -p /home/ec2-user/lzs-ai
            cd /home/ec2-user/lzs-ai
            
            # Initialize git if not already initialized
            if [ ! -d .git ]; then
              sudo git init
              sudo git remote add origin https://github.com/yourusername/lzs-corradai.git || true
            fi
            
            # Fix permissions
            sudo chown -R ec2-user:ec2-user /home/ec2-user/lzs-ai
            sudo chmod -R 755 /home/ec2-user/lzs-ai
            
            # Stop existing PM2 processes if any
            pm2 stop app || true
            pm2 delete app || true

      - name: Copy built files to EC2
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          source: ".output/,package.json,yarn.lock,prisma/"
          target: "/home/ec2-user/lzs-ai"
          strip_components: 0

      - name: Final EC2 Setup and Start
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd /home/ec2-user/lzs-ai
            
            # Fix permissions again after file copy
            sudo chown -R ec2-user:ec2-user /home/ec2-user/lzs-ai
            sudo chmod -R 755 /home/ec2-user/lzs-ai
            
            # Create .env file
            sudo tee .env > /dev/null << EOL
            NUXT_ENV=production
            DATABASE_URL="${{ secrets.DATABASE_URL }}"
            NUXT_ACCESS_TOKEN_SECRET="${{ secrets.NUXT_ACCESS_TOKEN_SECRET }}"
            NUXT_REFRESH_TOKEN_SECRET="${{ secrets.NUXT_REFRESH_TOKEN_SECRET }}"
            PORT=3000
            CHROMADB_URL="${{ secrets.CHROMADB_URL }}"
            APACHE_TIKA_URL="${{ secrets.APACHE_TIKA_URL }}"
            APP_URL="${{ secrets.APP_URL }}"
            SERVER=true
            EOL
            
            sudo chown ec2-user:ec2-user .env
            sudo chmod 644 .env
            
            # Install dependencies and start the application
            yarn install --frozen-lockfile --production
            
            # Generate Prisma client
            npx prisma generate
            
            pm2 start "yarn preview" --name app
            pm2 save 